const transformerType = require("./transformer-type");
const convertionType = require("./convertion-type");
const sinkerType = require("./sinker-type");
const collectorType = require("./collector-type");
const enricherType = require("./enricher-type");

module.exports = {
  transformerType,
  convertionType,
  sinkerType,
  collectorType,
  enricherType,
};
